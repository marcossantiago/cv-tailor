name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy-worker:
    name: Deploy Cloudflare Worker
    runs-on: ubuntu-latest
    permissions:
      contents: write
    outputs:
      worker_url: ${{ steps.deploy.outputs.deployment-url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Write worker/config.js from GitHub context
        run: |
          OWNER="${{ github.repository_owner }}"
          REPO="${{ github.event.repository.name }}"
          PAGES_ORIGIN="https://${OWNER}.github.io"

          # If the repo name is not the user/org root site, Pages serves under a subpath
          # but the Origin header is always just the scheme+host, so one entry covers all.
          cat > worker/config.js <<EOF
          // Auto-generated by the deploy pipeline.
          // Do not edit manually — changes will be overwritten on next deploy.
          export const ALLOWED_ORIGINS = [
            "${PAGES_ORIGIN}",
          ];
          EOF

      - name: Deploy with Wrangler
        id: deploy
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          workingDirectory: worker

      - name: Write config.js with worker URL
        run: |
          cat > config.js <<EOF
          // Auto-generated by the Cloudflare Worker deploy job.
          // Do not edit manually — changes will be overwritten on next deploy.
          const CONFIG = {
            workerUrl: "${{ steps.deploy.outputs.deployment-url }}",
          };
          EOF

      - name: Commit generated configs
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add config.js worker/config.js
          git diff --cached --quiet || git commit -m "chore: update generated configs [skip ci]"
          git push

  deploy-pages:
    name: Deploy to GitHub Pages
    runs-on: ubuntu-latest
    needs: deploy-worker
    permissions:
      contents: read
      pages: write
      id-token: write
    concurrency:
      group: pages
      cancel-in-progress: true
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          # Pull the latest commit (which includes the config.js update from deploy-worker)
          ref: main

      - name: Setup Pages
        uses: actions/configure-pages@v4

      - name: Upload artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: '.'

      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
